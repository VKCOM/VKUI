name: "Clear styleguide for closed PR"

on:
  pull_request:
    types: [closed]

jobs:
  clear_styleguide:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@2541b12 #v3.0.2
        with:
          ref: gh-pages
      - name: Remove styleguide
        run: rm -rf pull/${{ github.event.pull_request.number }}
      - name: Unpublish styleguide
        uses: stefanzweifel/git-auto-commit-action@49620cd #v4.14.1
        with:
          commit_message: "Remove PR ${{ github.event.pull_request.number }}"
          branch: gh-pages

  patch:
    if: github.event.pull_request.merged == true && contains(github.event.pull_request.labels.*.name, 'patch')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@2541b12 #v3.0.2
      - uses: actions/setup-node@eeb10cf #v3.3.0
        with:
          node-version: 14
          cache: "yarn"
      - run: yarn install --frozen-lockfile --ignore-scripts
      - name: Set Git credentials
        run: |
          git config --local user.email "actions@github.com"
          git config --local user.name "GitHub Action"
      - run: node ./.github/gitflow/patch.js
        env:
          GITHUB_PULL_NUMBER: ${{ github.event.number }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

{
  "private": true,
  "name": "@project-tools/postcss-restructure-variable",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "node": "node --experimental-transform-types",
    "build": "echo no build",
    "test": "yarn run node --test '**/*.test.*'",
    "test:update-snapshots": "yarn run node --test-update-snapshots --test '**/*.test.*'",
    "test:ci": "yarn test"
  }
}

{
  "private": true,
  "name": "@project-e2e/helpers",
  "description": "Хелперы для тестов jest + playwright в пакете 'packages/vkui'. В браузерной сборке подменяются на 'browser/mount'.",
  "version": "1.0.0",
  "types": "index.ts",
  "scripts": {
    "build": "echo no build",
    "test": "echo no test",
    "test:ci": "echo no test:ci"
  }
}

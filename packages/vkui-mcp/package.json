{
  "name": "@vkontakte/vkui-mcp",
  "version": "0.0.0",
  "description": "MCP server for VKUI documentation",
  "repository": {
    "type": "git",
    "url": "https://github.com/VKCOM/VKUI.git",
    "directory": "packages/vkui-mcp"
  },
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "bin": {
    "vkui-mcp": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepack": "yarn run build",
    "test": "yarn build && yarn run -T vitest run",
    "test:ci": "yarn run -T vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.25.3",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}

import { describe } from 'vitest';
import { baselineComponent } from '../../testing/utils';
import { FormLayoutGroup } from './FormLayoutGroup';

describe('FormLayoutGroup', () => {
  baselineComponent(FormLayoutGroup);
});

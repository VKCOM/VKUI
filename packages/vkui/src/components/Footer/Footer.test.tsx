/**
 * jest-runners-groups
 * @group unit
 */

import { baselineComponent } from '../../testing/utils';
import { Footer } from './Footer';

describe('Footer', () => {
  baselineComponent(Footer);
});

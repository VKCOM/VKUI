import { describe } from 'vitest';
import { baselineComponent } from '../../../testing/utils';
import { FormItemTopLabel } from './FormItemTopLabel';

describe('FormItemTopLabel', () => {
  baselineComponent(FormItemTopLabel);
});

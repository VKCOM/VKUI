---
description: Компонент для отображения основного наборного текста интерфейса. Используется для большинства текстовых элементов - описаний, подписей ячеек, контента карточек и т. д.
---

<Overview group="typography" forcedName="Typography-Text" forcedPath="components/Typography/Text/Text.tsx">

# Text [tag:component]

Компонент для отображения основного наборного текста интерфейса.
Используется для большинства текстовых элементов: описаний, подписей ячеек, контента карточек и т. д.

</Overview>

{/* @example-description: Базовое использование компонента `Text`. */}
<Playground>
  ```jsx
  <Text>Основной текст интерфейса</Text>
  ```
</Playground>

## Базовые свойства

Компонент поддерживает все [базовые свойства типографики](/components/typography#base-props):
`weight`, `useAccentWeight`, `normalize`, `Component`, `inline`, `align`.

`Text` не имеет уникальных свойств — вся кастомизация осуществляется через базовые свойства типографики.

## Свойства и методы [#api]

<PropsTable name="Typography/Text/Text" />

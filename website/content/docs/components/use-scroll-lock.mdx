<Overview>

# useScrollLock

Хук для блокировки прокрутки страницы. Полезен при открытии модальных окон, попапов и других элементов, которые требуют ограничения взаимодействия с фоном.

</Overview>

## Пример использования

```jsx static
const App = () => {
  const [opened, setOpened] = React.useState(false);

  // Блокируем скролл при открытии попапа
  useScrollLock(opened);

  return (
    <div>
      <h1>My Awesome App</h1>
      <label htmlFor="toggle">Toggle popout</label>
      <input
        id="toggle"
        type="checkbox"
        checked={opened}
        onChange={(e) => setOpened(e.target.checked)}
      />
      {opened && <PopoutWrapper>Some content</PopoutWrapper>}
    </div>
  );
};
```

## Требования

- 🚨 Ваше приложение должно быть обернуто в `<AppRoot>`, так как именно через этот компонент:
  - Определяется контекст для управления скроллом
  - Устанавливается уровень блокировки (полезно для вложенных модалок)

## Особенности

- Автоматически снимает блокировку при размонтировании компонента
- Поддерживает вложенные блокировки (несколько модалок одновременно)

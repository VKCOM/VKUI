services:
  benchmark:
    image: ${IMAGE}
    ipc: host
    user: root
    working_dir: /repo/benchmark
    command: sh -c "
      YARN_ENABLE_SCRIPTS=false yarn install --immutable &&
      yarn run browser:benchmark:ci ${UPDATE_SNAPSHOTS_FLAG:-}
      "
    volumes:
      - ../:/repo
      # Исключаем node_modules.
      - /repo/node_modules
      - /repo/benchmark/node_modules
      # Кешируем установленные внутри контейнера node_modules и кэш директории.
      - benchmark_yarn_cache:/yarn
      - benchmark_root_node_modules_cache:/repo/node_modules
      - benchmark_node_modules_cache:/repo/benchmark/node_modules
      - benchmark_playwright_cache:/repo/benchmark/playwright/.cache
      # Исключаем всё то, что не потребуется в контейнере.
      - /repo/.git
      - /repo/.husky
      - /repo/.cache
      - /repo/.swc
      - /repo/packages/vkui/.swc

volumes:
  benchmark_yarn_cache:
  benchmark_root_node_modules_cache:
  benchmark_node_modules_cache:
  benchmark_playwright_cache:

Компонент, прокидывающий вниз данные об [адаптивности](#!/Adaptivity). Изначально, рассчитывает `sizeX`, `sizeY` и 
`viewWidth` самостоятельно, основываясь на `window`. Все эти 4 сущности можно переопределить, передав их в 
качестве свойств. 
